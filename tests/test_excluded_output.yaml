- container:
      name: "httpd:2.4"
      arguments:
          auto_remove: True
          ports:
              80: 80
  tests:
    - name: "Good"
      command: "wget -O - http://localhost/"
      excluded-output: "It doesn't work!"
    - name: "Bad"
      command: "wget -O - http://localhost/"
      excluded-output: "It works!"
